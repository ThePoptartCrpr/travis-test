language: generic
before_install:
    - chmod +x ./scripts/add-changelog.sh
    - chmod +x ./scripts/push.sh
    - chmod +x ./scripts/release-build.sh
    - export AUTHOR_NAME="$(git log -1 $TRAVIS_COMMIT --pretty="%aN")"
script:
    - ./scripts/release-build.sh